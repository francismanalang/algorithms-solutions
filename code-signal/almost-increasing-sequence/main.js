// eslint-disable-next-line no-unused-vars
function solution(sequence) {
  const min = Math.min(...sequence);
  // eslint-disable-next-line no-empty
  for (let i = min; i < sequence.length; i++) {

  }
}
